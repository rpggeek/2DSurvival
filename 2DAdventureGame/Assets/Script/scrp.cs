using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scrp : MonoBehaviour {

	// Use this for initialization
	//GameObject obj;
	void Start () {
		//obj = GameObject.Find ("Character");
	}
	
	// Update is called once per frame
	void Update () {
		//obj.transform.position = Camera.main.ScreenToWorldPoint (new Vector3 (Screen.width / 2, Screen.height / 2, Camera.main.nearClipPlane));
	}
}
